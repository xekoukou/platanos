

This a graph processing system that will eventually also be a database so as to store the vertex state.

Work in progress.

In order to compile it:

1)create a build dir

2)cmake ..

3)make


Testing it requires a zookeeper-server to connect to.
